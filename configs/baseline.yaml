model:
  provider: offline
  model_name: deterministic-baseline
  temperature: 0.0

retrieval:
  top_k: 5
  vector_store: lexical
  reranking: false

defenses:
  context_boundary: false
  retrieval_sanitizer: false
  pii_redaction: false
  output_validator: false
  tool_policy_gate: false
  tenant_filtering: false

logging:
  jsonl: true
  save_retrieved_chunks: true
  save_policy_decisions: true
