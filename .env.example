# Optional provider settings for future LLM-backed runs.
# The current prototype can run fully offline with deterministic mock generation.
OPENAI_API_KEY=
LITELLM_MODEL=
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=
